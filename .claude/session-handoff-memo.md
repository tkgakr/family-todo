# Family Todo アプリ - 次セッション引き継ぎメモ

## 🔍 現在の状況
- **プロジェクト**: Family Todoアプリ（フロントエンド: React+TypeScript、バックエンド: Rust）
- **作業ディレクトリ**: `/Users/atakagi/github/tkgakr/family-todo-claude/frontend`
- **Git状態**: cleanブランチ、main から派生した claude ブランチで作業中

## ✅ 完了済み
1. プロジェクト構造とpackage.json確認完了
2. フロントエンドの依存関係インストール完了（`npm install`）
3. バックエンドのCargo.toml基本修正完了：
   - `resolver = "2"` 追加
   - `aws-lambda-events` → `aws_lambda_events` 修正
   - `opentelemetry_sdk` features追加
   - 空の`snapshot-manager`ディレクトリ削除

## ⚠️ 修正が必要な項目

### フロントエンド
- **リントエラー（24個）**:
  - `button` 要素に `type="button"` 属性追加が必要
  - 未使用変数の修正（例: `TodoDetail.tsx:119` の `index`）
  - 自己終了タグの修正
  - label要素にhtmlFor属性追加

- **TypeScriptエラー**:
  - `import.meta.env` の型定義不足
  - Vite環境変数型定義ファイル（`vite-env.d.ts`）が必要

### バックエンド
- **主要コンパイルエラー**:
  - AWS Lambda Events型不一致（HeaderMap、Body型）
  - Response構造体の型定義問題
  - 未使用import多数
  - telemetryモジュールは修正済み

## 🚧 次セッションでの作業手順

### 1. フロントエンド修正
```bash
cd /Users/atakagi/github/tkgakr/family-todo-claude/frontend

# 1. Vite環境変数型定義作成
# src/vite-env.d.ts に interface ImportMetaEnv 追加

# 2. リントエラー修正
npm run lint  # エラー確認
# button要素にtype="button"追加
# 未使用変数を_prefixまたは削除

# 3. ビルド確認
npm run build

# 4. テスト実行
npm test
```

### 2. バックエンド修正
```bash
cd /Users/atakagi/github/tkgakr/family-todo-claude/backend

# 主要修正箇所:
# - query-handler/src/responses.rs
# - command-handler/src/responses.rs 
# - 各handlerの未使用import削除

make lint
```

### 3. 修正後の最終確認
```bash
cd /Users/atakagi/github/tkgakr/family-todo-claude
make test    # 全テスト実行
make build   # 全ビルド確認
```

## 📁 重要なファイル
- `backend/Cargo.toml` - ワークスペース設定（修正済み）
- `frontend/package.json` - フロントエンド依存関係
- `Makefile` - プロジェクト全体のコマンド定義

## 🎯 最終目標
全てのリント、ビルド、テストが通る状態にして、SOLID原則に従った清潔なコードベースを実現する。

---
**作成日時**: 2025-08-31
**次回作業**: フロントエンドのリントエラー修正から開始