[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
lambda_http = "0.15.1"
http = "1.0.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

tokio = { version = "1", features = ["macros", "rt-multi-thread"] }

[dev-dependencies]
http = "1.0.0"
